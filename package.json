{
  "name": "chroma-paper-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test src/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
